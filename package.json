{
  "name": "ai-hrms",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "check": "powershell -ExecutionPolicy Bypass -File harness/check.ps1",
    "web:dev": "npm --workspace apps/web run dev",
    "web:build": "npm --workspace apps/web run build",
    "web:check": "npm --workspace apps/web run check"
  }
}
